---
"@proxlane/web": patch
---

Sit the wordmark's ring on the baseline where the real `o` sits, derived from the face's
measured metrics rather than nudged by eye, and add a social card built from the route diagram.
