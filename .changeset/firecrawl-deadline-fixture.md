---
"@proxlane/adapters": patch
---

Firecrawl gains its recorded `deadline` fixture, so the contract tests and the weekly drift check cover the gateway's own deadline firing mid-request for every adapter.
