---
'@proxlane/web': minor
---

A migration page family at `/migrate`, with its first page: migrate from ScraperAPI. The one-line hostname change, the parameter map read from the adapter's own translation step (including the seven ScraperAPI parameters the gateway ignores today, and how `X-Ignored-Params` names them), the credit multipliers as documented on 2026-08-31, and what changes for the better. `content:lint` now covers every page family under `content/`, not only symptoms, and `docs:check` resolves internal links against every route on disk rather than `/docs/*` alone.
