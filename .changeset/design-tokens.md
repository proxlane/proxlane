---
"@proxlane/ui": minor
---

The hand-authored token layer from `design.md`, light and dark built in the same pass, with `tokens:check` enforcing the palette, per-role contrast on both grounds, and no raw hex outside the token file.
