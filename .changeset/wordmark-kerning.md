---
"@proxlane/web": patch
---

Match the wordmark's ring to the real `o`: an ellipse at the measured ink width rather than a
circle at the o's height, with the font's own `ro` and `ox` kerning baked into the margins.
