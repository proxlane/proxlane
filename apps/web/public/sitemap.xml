<?xml version="1.0" encoding="UTF-8"?>
<!-- One route today. It is here anyway because it names the canonical host, which is the
     part that matters while the site is reachable on both proxlane.dev and a workers.dev
     subdomain. `apps/web/src/generators/**` takes this over when there are generated pages. -->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://proxlane.dev/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
